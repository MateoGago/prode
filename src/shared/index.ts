// shared: cross-feature utilities
// - datetime: formatAR() — converts UTC instants to America/Argentina/Buenos_Aires display
// - supabase client factory (server + browser)
// - common TypeScript types
// - UI primitives shared across features
export {};
