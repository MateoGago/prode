// Cancha Pop shared animation layer. Components import directly from "motion/react"
// for primitives (motion, AnimatePresence); this barrel only exposes presets/variants.
export {
  bounceEase,
  popSpring,
  rise,
  riseTransition,
  staggerContainer,
} from "./presets";
export { useReveal } from "./use-reveal";
